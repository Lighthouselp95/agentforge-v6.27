{
  "name": "agentforge",
  "version": "7.0.6",
  "main": "dist-electron/main.js",
  "bin": "dist/server.js",
  "description": "ACP Multi-Agent Orchestrator with GUI",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "dev:watch": "tsx watch --exclude \"data/**/*\" src/server.ts",
    "build": "tsc && vite build --config web/vite.config.ts && tsc -p tsconfig.electron.json",
    "build:electron": "npm run build && npm run build:exe && electron-builder --win portable",
    "build:exe": "npm run build && node scripts/build-sea-bundle.mjs && node scripts/gen-sea-assets.mjs && node --experimental-sea-config sea-config.json && node -e \"const fs=require('fs');fs.mkdirSync('release',{recursive:true});fs.copyFileSync(process.execPath,'release/agentforge-web.exe')\" && npx postject release/agentforge-web.exe NODE_SEA_BLOB dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "electron:dev": "concurrently -n server,web,electron -c blue,green,magenta \"npm run dev:watch\" \"npm run dev:web\" \"wait-on http://localhost:4001 && npx electron dist-electron/main.js\"",
    "start": "tsx src/server.ts",
    "dev:web": "vite --config web/vite.config.ts",
    "dev:all": "concurrently -n server,web -c blue,green \"npm run dev:watch\" \"npm run dev:web\"",
    "approve": "npm approve-scripts esbuild electron",
    "clean:cache": "npm cache clean --force"
  },
  "build": {
    "appId": "com.agentforge.app",
    "productName": "AgentForge",
    "asar": true,
    "npmRebuild": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "dist-electron/**/*",
      "src/**/*",
      "data/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "start.bat",
        "to": "start.bat"
      },
      {
        "from": "start.sh",
        "to": "start.sh"
      },
      {
        "from": "release/agentforge-web.exe",
        "to": "agentforge-web.exe"
      }
    ],
    "win": {
      "target": [
        "portable"
      ]
    },
    "portable": {
      "artifactName": "AgentForge-Portable.exe"
    }
  },
  "pkg": {
    "entry": "dist/server.js",
    "targets": [
      "node20-win-x64"
    ],
    "outputPath": "release",
    "assets": [
      "dist/index.html",
      "web/dist/**/*"
    ],
    "compress": "GZIP"
  },
  "dependencies": {
    "express": "^4.18.0",
    "tsx": "^4.0.0",
    "uuid": "^9.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "@types/ws": "^8.5.0",
    "@yao-pkg/pkg": "^6.22.0",
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0",
    "electron": "^30.5.1",
    "electron-builder": "^24.13.3",
    "postject": "^1.0.0-alpha.6",
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "wait-on": "^9.1.0"
  }
}
